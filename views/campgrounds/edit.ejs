<% include ../partials/header %>

<div class="col-md-6 offset-md-3">
        <h1>Edit <%=campgrounds.name%></h1>
        <form action="/campgrounds/<%=campgrounds._id%>?_method=PUT" method="POST">

                <div class="form-group">
                        <label for="camp[name]">Name</label>
                        <input class="form-control" name="camp[name]"type="text" value="<%=campgrounds.name%>">
                </div>

                <div class="form-group">
                    <label for="camp[image]">Image</label>
                    <input class="form-control" name="camp[image]"type="text" value="<%=campgrounds.image%>">
                </div>
                
                <div class="form-group">
                    <label for="camp[desc]">Description</label>
                    <textarea class="form-control" name="camp[desc]"rows="10"><%=campgrounds.desc%></textarea>
                </div>



                <button type="submit" class="btn btn-primary btn-block">Submit!</button>
        </form>
        <form action="/campgrounds/<%=campgrounds._id%>?_method=DELETE" method="POST">
            <button class="btn btn-danger">Delete</button>
        </form>
        <a href="/campgrounds">Go Back</a>
</div>

<% include ../partials/footer %>